name: "My First Workflow"
on: 
  push:
    branches: 
      - main
  workflow_dispatch:
jobs: 
  MyFirstJob:
    runs-on: ubuntu-latest
  
    steps:
      - run: echo "Hello World"

      - name: Checkout
        uses: actions/checkout@v4.2.2

      - name: List files in Repos
        run: |
            echo "the repository: ${{ github.reopsitory }} contains the following files:"
            tree
